-- config model



return {

    install_dir = _env('om_PATH') or _home() .. '/om', -- cwm install dir / cwm 安装目录
    language = 'zh', -- en [english] or zh [中文]
    api_host = 'http://127.0.0.1:8008'
}
